<% content_for :title, "Editando Venda" %>

<div class="main-card-container">
  <div class="page-header-row">
    <h1>Editando Venda</h1>
    <div>
      <%= link_to "Ver Venda", @venda, class: "new-prod-btn", style: "background-color: #6b8e23; margin-right: 10px;" %>
      <%= link_to "Voltar para vendas", vendas_path, class: "new-prod-btn", style: "background-color: #B78F7A;" %>
    </div>
  </div>

  <%= render "form", venda: @venda %>
</div>
